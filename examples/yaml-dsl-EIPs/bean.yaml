- route:
    id: route-example-bean
    from:
      uri: kafka:example-source-topic
      steps:
        - bean:
            beanType: com.example.myBean
            method: myMethod
        - to:
            uri: mock:result
