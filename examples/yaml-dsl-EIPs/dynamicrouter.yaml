- route:
    id: route-example-dynamic-router
    from:
      uri: kafka:example-source-topic
      steps:
        - dynamicRouter:
            expression:
              method:
                method: slip
                beanType: com.foo.MySlipBean
        - to:
            uri: mock:result
