- route:
    id: route-example-loop-xpath
    from:
      uri: kafka:example-source-topic
      steps:
        - loop:
            expression:
              xpath:
                expression: /hello/@times
            steps:
              - to:
                  uri: mock:result
