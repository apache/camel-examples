- route:
    id: route-example-convert-body-to
    from:
      uri: kafka:example-source-topic
      steps:
        - convertBodyTo:
            type: String
        - log:
            message: 'The message content: ${body}'
