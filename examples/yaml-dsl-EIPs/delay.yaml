- route:
    from:
      uri: kafka:example-source-topic
      steps:
        - delay:
            asyncDelayed: true
            expression:
              constant:
                expression: '3'
        - to:
            uri: mock:result
