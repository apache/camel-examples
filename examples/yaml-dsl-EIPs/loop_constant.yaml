- route:
    id: route-example-loop-constant
    from:
      uri: kafka:example-source-topic
      steps:
        - loop:
            expression:
              constant:
                expression: '8'
            steps:
              - to:
                  uri: mock:result
