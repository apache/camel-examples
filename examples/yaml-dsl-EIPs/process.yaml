- route:
    id: route-example-process
    from:
      uri: kafka:example-source-topic
      steps:
        - process:
            ref: '#class:com.foo.MyProcessor'
        - to:
            uri: mock:result
