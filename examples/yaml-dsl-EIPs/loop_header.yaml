- route:
    id: route-example-loop-header
    from:
      uri: kafka:example-source-topic
      steps:
        - loop:
            expression:
              header:
                expression: loop
            steps:
              - to:
                  uri: mock:result
