- route:
    id: route-example-idempotentconsumer
    from:
      uri: kafka:example-source-topic
      steps:
        - idempotentConsumer:
            idempotentRepository: MemoryIdempotentRepository
            expression:
              simple:
                expression: ${body}
        - to:
            uri: mock:result
